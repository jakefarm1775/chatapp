version: '3.8'
services: 
  chatapptestauto:
    image: jakefarm1775/chatapptest:latest
    build: .
    container_name: chatapp0101
    ports:
      - "80:3000"
    environment:
      - PORT=3000
    volumes:
      - ./:/app
    command: ["node", "index.js"]
